# =============================================================================
# Infrastructure AWS – Projet personnel – 2025
# =============================================================================
# Fichiers séparés par domaine :
#   - versions.tf
#   - variables.tf / outputs.tf
#   - alb.tf / ec2.tf / s3.tf / network.tf / iam.tf / ssh.tf
#
# Pour lancer : terraform init && terraform apply
# =============================================================================